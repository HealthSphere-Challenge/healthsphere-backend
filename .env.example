# Documentation template only: no runtime exists yet. No credentials supplied.
# SQLAlchemy/psycopg 3 URL form: postgresql+psycopg://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=
# Must identify a separately provisioned test database; never fall back to DATABASE_URL.
TEST_DATABASE_URL=
# Backend-only internal destinations; choose origins during foundation/integration.
AI_SERVICE_URL=
AGENT_SERVICE_URL=
# Auth/session, CORS and service-auth settings await their recorded decisions.
